import WeatherPage from '@/components/weather/weatherPage'; // Import the main weather page component

// Home component that renders the WeatherPage component
export default function Home() {
  return <WeatherPage />; // Return the WeatherPage component
}
